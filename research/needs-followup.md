# Needs follow-up

Sources that could not be accessed (paywalled, geo-blocked, login-gated, or
otherwise unreachable) during research. For each entry: the row it affects,
the URL attempted, why it failed, and what was done instead (never a guess
from memory presented as verified).

Format per entry:

```
## <Country/Region> — <instrument name>
- URL attempted:
- Failure reason:
- Row confidence set to: unverified — source inaccessible
- Alternative source used (if any):
```
